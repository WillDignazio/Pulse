package net.digitalbebop;

interface App {
    /**
     * Initialize the application, this function will be called by the App
     * bootstrapper on startup. This is synonymous with the main() method java
     * would call.
     */
    void init();
}
